module.exports = {
    mode: 'jit',
    purge: ['./src/**/*.svelte'],
    theme: {
        extend: {
            fontFamily: {
                sans: ['GT Walsheim', 'sans-serif'],
            },
        }
    },
    variants: {
        extend: {},
    },
    plugins:[],
};